query Entity($id: String!){
	entity(id: $id){
		id
		name
		shortName
		abreviations
		description
		category {
			id
			reflink
		}
		biomarkers {
			id
			entityIds
			entities {
				id
				name
				shortName
			}
		}
	} 
}